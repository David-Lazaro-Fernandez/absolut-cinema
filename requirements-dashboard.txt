# Solo para el dashboard. El scraper y analytics/ no necesitan nada fuera de la librería estándar.
streamlit>=1.60          # st.Page(visibility=), st.iframe, width="stretch"
pandas>=2.2
watchdog>=4
boto3>=1.35              # correo por Amazon SES (AC_MAIL_BACKEND=ses); en desarrollo no se usa
