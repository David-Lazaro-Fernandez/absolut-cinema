# Generado por `python3 -m jobs.units write` desde jobs/registry.py; no se edita a mano.
[Unit]
Description=absolut-cinema: seats, cada hora a :50

[Timer]
OnCalendar=*-*-* *:50:00
Persistent=true
AccuracySec=30s

[Install]
WantedBy=timers.target
